# next-authはnodemailer（未使用のEmailプロバイダ機能用）にpeerOptional依存を持つが、
# バージョンが古く既知の脆弱性がある(^7.0.7)。このプロジェクトはnext-authのEmail
# プロバイダを使わず、独自のpatient-magiclink Credentialsプロバイダでメール認証を
# 実装しているため、修正済みのnodemailer(^9.0.3)を直接使う。この設定がないと
# Vercelのビルド時に`npm install`がpeer依存の競合でエラーになる。
legacy-peer-deps=true
